/**
 * Stub file for Catch's test runner.
 * 
 * Having this in a separate file speeds up compilation, as macros only have to
 * be expanded once, rather than once per target.
 */

#define CATCH_CONFIG_MAIN
#include <catch.hpp>
